# PyInstaller output
build/
dist/
*.spec
__pycache__/
*.py[cod]

# The installed copy lives here at the repo root, which is also what makes
# this a valid install folder: Install-GcsViewer.ps1 registers whatever
# GCSViewer.exe sits beside it.  _internal is the folder build's library
# payload, ~70 MB across 1,043 files.
/GCSViewer.exe
/_internal/

# personal notes, the superseded C# launcher, and the archived 2026-06-10 exe
local/

# design files and renders - the only ones the repo carries are its own demo
*.gcs
*.gem
*.png
!docs/demo.gcs
!docs/demo.png
!docs/icon-preview.png
*_views.png

# packaged distribution
*.zip

# editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
