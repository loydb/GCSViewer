# PyInstaller output
build/
dist/
*.spec
__pycache__/
*.py[cod]

# the live executable the .gcs association points at - rebuilt from source by
# scripts/build_exe.py and published on the releases page.  /GCSViewer/ is the
# installed folder build, ~70 MB across 1,043 files; /GCSViewer.exe is the
# single-file shape, kept only if that is what is installed
/GCSViewer.exe
/GCSViewer/

# personal notes, the superseded C# launcher, and the archived 2026-06-10 exe
local/

# design files and renders - the only ones the repo carries are its own demo
*.gcs
*.gem
*.png
!docs/demo.gcs
!docs/demo.png
!docs/icon-preview.png
*_views.png

# packaged distribution
*.zip

# editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
